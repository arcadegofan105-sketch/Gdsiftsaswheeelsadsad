# Колесо Фортуны - Telegram Web App

Игра "Колесо Фортуны" с интеграцией Telegram Mini App и возможностью выигрыша подарков.

## Технологии

- **Frontend**: HTML, CSS, JavaScript, Telegram Web App API
- **Backend**: Node.js, Express
- **База данных**: SQLite + Prisma ORM
- **Деплой**: Render (backend), Netlify (frontend)

## Структура проекта

```
├── backend/           # Backend сервер
│   ├── prisma/       # Схема базы данных
│   ├── server.js     # Основной файл сервера
│   └── package.json
├── index.html        # Главная страница игры
├── script.js         # Логика frontend
├── style.css         # Стили
└── .gitignore        # Игнорируемые файлы
```

## Настройка локальной разработки

### 1. Клонирование репозитория

```bash
git clone https://github.com/arcadegofan105-sketch/Gdsiftsaswheeelsadsad.git
cd Gdsiftsaswheeelsadsad
```

### 2. Настройка backend

```bash
cd backend
npm install
```

### 3. Создание файла .env

Создайте файл `backend/.env` со следующими переменными:

```env
# База данных
DATABASE_URL="file:./dev.db"

# Telegram Bot
BOT_TOKEN=ваш_токен_от_BotFather

# URL вашего Web App
WEB_APP_URL=https://ваш_домен.netlify.app/

# Порт сервера
PORT=3001

# Админы (опционально)
ADMIN_TELEGRAM_IDS="123456789,987654321"
```

### 4. Инициализация базы данных

```bash
npx prisma generate
npx prisma migrate dev
```

### 5. Запуск сервера

```bash
node server.js
```

## Деплой

### Backend (Render)

1. Создайте новый Web Service на [Render](https://render.com)
2. Подключите GitHub репозиторий
3. Настройте переменные окружения:
   - `BOT_TOKEN`
   - `DATABASE_URL`
   - `WEB_APP_URL`
   - `PORT`
   - `ADMIN_TELEGRAM_IDS`
4. Render автоматически задеплоит приложение

### Frontend (Netlify)

1. Создайте новый сайт на [Netlify](https://netlify.com)
2. Подключите GitHub репозиторий
3. Настройте:
   - Build command: (оставьте пустым)
   - Publish directory: `/`
4. Netlify автоматически задеплоит фронтенд

## Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Настройте Web App через `/newapp`
4. Укажите URL вашего Netlify сайта

## Безопасность

⚠️ **ВАЖНО**: 
- Никогда не коммитьте файл `.env` в Git
- Используйте `.gitignore` для исключения конфиденциальных файлов
- Храните токены бота и API ключи в переменных окружения

## Функциональность

- 🎰 Вращение колеса фортуны
- 🎁 Выигрыш подарков
- 💎 Система баллов
- 📊 Сохранение истории игр
- 👥 Интеграция с Telegram

## Лицензия

MIT
